import SwiftUI

/// Colores de la Live Activity. La extensión no comparte el catálogo de la app, así que se definen aquí.
nonisolated enum LiveActivityPalette {
    static let lime = Color(red: 0.843, green: 0.953, blue: 0.416)
    static let lavender = Color(red: 0.725, green: 0.659, blue: 0.973)
    static let onAccent = Color(red: 0.039, green: 0.055, blue: 0.082)
    static let background = Color(red: 0.039, green: 0.055, blue: 0.082)
    static let surface = Color(red: 0.129, green: 0.149, blue: 0.192)
}
